<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme.BottomNav" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemBackground">@android:color/transparent</item> <!-- Прозрачный фон -->
        <item name="itemRippleColor">@android:color/transparent</item> <!-- Убираем ripple-эффект -->
        <item name="android:fitsSystemWindows">true</item>
    </style>
</resources>